#!/bin/sh
USER=dh_eppcxj
HOST=haines.dreamhost.com
DIR=anhack.xyz/

BUILD_FLAGS=--minify

# clean build
rm -rf public/*
hugo ${BUILD_FLAGS} && rsync -avz --delete public/ ${USER}@${HOST}:~/${DIR}

exit 0
